{
  "name": "api-server",
  "version": "0.0.0",
  "description": "Backend for processing tracking data and handling API requests.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "eamongordon",
  "dependencies": {
    "koa": "^2.15.3"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
